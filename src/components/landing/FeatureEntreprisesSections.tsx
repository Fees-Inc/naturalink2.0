import { RecognitionSection } from "@/components/landing/RecognitionSection";

/** Conservé pour compatibilité d'import — préférez `RecognitionSection` dans les nouvelles pages. */
export function FeatureEntreprisesSections() {
  return <RecognitionSection />;
}
